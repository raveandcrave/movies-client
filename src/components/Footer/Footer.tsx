import {FC} from 'react';
import {Layout} from 'antd';

import './style.scss';

const Footer: FC = () => {
  return <Layout.Footer className="footer">ПАНКИ ХОЙ!!</Layout.Footer>;
};

export default Footer;
